[project]
name = "faceextractor"
description = "Custom Node to detect face matching the input reference image."
version = "1.0.0"
license = {file = "LICENSE"} 
# classifiers = [
#     # For OS-independent nodes (works on all operating systems)
#     "Operating System :: OS Independent",
#
#     # OR for OS-specific nodes, specify the supported systems:
#     "Operating System :: Microsoft :: Windows",  # Windows specific
#     "Operating System :: POSIX :: Linux",  # Linux specific
#     "Operating System :: MacOS",  # macOS specific
#
#     # GPU Accelerator support. Pick the ones that are supported by your extension.
#     "Environment :: GPU :: NVIDIA CUDA",    # NVIDIA CUDA support
#     "Environment :: GPU :: AMD ROCm",       # AMD ROCm support
#     "Environment :: GPU :: Intel Arc",      # Intel Arc support
#     "Environment :: NPU :: Huawei Ascend",  # Huawei Ascend support
#     "Environment :: GPU :: Apple Metal",    # Apple Metal support
# ]

dependencies = ["# Face Extractor Requirements", "# ===========================", "# Core dependencies", "numpy>=1.20.0", "opencv-python>=4.5.0", "torch>=1.9.0", "Pillow>=8.0.0", "psutil>=5.8.0", "# =============================================================================", "# DETECTION BACKENDS - Choose based on your license requirements", "# =============================================================================", "# -----------------------------------------------------------------------------", "# OPTION 1: InsightFace (BEST QUALITY - NON-COMMERCIAL ONLY!)", "# -----------------------------------------------------------------------------", "# InsightFace provides the most accurate face detection and recognition.", "# However, it is licensed for NON-COMMERCIAL USE ONLY.", "# See: https://github.com/deepinsight/insightface#license", "#", "# To install:", "# pip install insightface onnxruntime-gpu", "#", "# For CPU only:", "# pip install insightface onnxruntime", "insightface>=0.7.3", "onnxruntime-gpu>=1.15.0", "# -----------------------------------------------------------------------------", "# OPTION 2: MediaPipe (GOOD QUALITY - COMMERCIAL OK)", "# -----------------------------------------------------------------------------", "# MediaPipe is Google's ML solution, licensed under Apache 2.0.", "# Safe for commercial use. Good detection quality.", "#", "# To install:", "# pip install mediapipe", "mediapipe>=0.10.0", "# -----------------------------------------------------------------------------", "# OPTION 3: OpenCV Cascade (BASIC - COMMERCIAL OK)", "# -----------------------------------------------------------------------------", "# Built into OpenCV, no additional install needed.", "# Licensed under BSD. Safe for commercial use.", "# Lower accuracy than InsightFace/MediaPipe but always available.", "# (Already included in opencv-python)"]

[project.urls]
Repository = "https://github.com/llikethat/ComfyUI-faceExtractor"
#  Used by Comfy Registry https://registry.comfy.org
Documentation = "https://github.com/llikethat/ComfyUI-faceExtractor/wiki"
"Bug Tracker" = "https://github.com/llikethat/ComfyUI-faceExtractor/issues"

[tool.comfy]
PublisherId = ""
DisplayName = "ComfyUI-faceExtractor"
Icon = ""
includes = [] 
# "requires-comfyui" = ">=1.0.0"  # ComfyUI version compatibility

